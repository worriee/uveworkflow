Update [6-1-26]:

-has project memory file that stores current context session (since most ai context window is too small, the ai always forgets)

-error memory file (stores and updates current errors for better debugging moments 😆)

-different personas that act simultaneously eg. planner, coder, debugger, ask, and 
orchestrator (acts all of them at once that depends on the user's prompt).

- ".clinerules & .system_instructions" files is for overall system general rules so each files is related and works to each other.

-good for portability you can retain the context of your current project and use it to other ai such as roo code & cursor.
//but for now I only used cline so will update this soon if I have enough time.

How to?
Just clone the folder and copy to root directory of your current project.